# Automatically generated by espanso migration tool
# Original file: default.yml

matches:
  - trigger: ":ss"
    image_path: "/tmp/screenshot.png"

  - trigger: ":ci"
    image_path: "/home/wynand/work-sync/Support_flows/Support_flows/enc_charge.jpeg"

  - trigger: "eml"
    replace: "wynandgouwswg@gmail.com"

  - trigger: mcmid
    replace: MyCricket matchID

  - trigger: ":today"
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "%m/%d/%Y"

  - trigger: ":shell"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: echo Hello from your shell

  - trigger: ":ip"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: "curl 'https://api.ipify.org'"
          shell: wsl

  - trigger: ":booking"
    replace: |
      https://outlook.office365.com/owa/calendar/FrogBox@SportradarAG.onmicrosoft.com/bookings/
    force_clipboard: true

  - trigger: ":bluescreen"
    replace: "You're sending us a bluescreen. This usually means either the camera is off, or there's a loose HDMI cable somewhere.\n\nCan you also check the HDMI cable goes Camera -> Input -> Output -> Encoder"
    force_clipboard: true

  - trigger: ":longblue"
    replace: "We are seeing a blue screen on your feed. Can you please check the following\n1) The camera is on and plugged into the external battery\n2) The external battery is on\n3) The HDMI chain goes as follows\n  Camera -> Repeater Input\n  Repeater Output -> Long HDMI -> Encoder\nYou may need to unplug and re-plug some of the cables to ensure they're sitting tightly.\n"
    force_clipboard: true

  - trigger: ":reboot"
    replace: "Can you please reboot your encoder, then let us know when you have a solid blue 4G light on the encoder again"

  - trigger: ":offline"
    replace: "We cant see your encoder online at the moment, do you have a solid blue 4G light?\n\nIf it is can you give the encoder a quick restart please and let us know when it has a solid blue 4G light?"
    force_clipboard: true

  - trigger: ":charging"
    replace: Can you please check your encoder is connected to the external battery and the battery is on. The battery icon LED on the encoder should be either red or green to indicate it is charging

  - trigger: ":nodata"
    replace: Are you able to double check your sim has data on it? You will need ~1gb per hour to stream

  - trigger: ":useviewer"
    replace: "The easiest way to check the video is to unplug the HDMI cable from the encoder and temporarily plug it into the viewer. Then when you have a picture on the viewer, unplug the HDMI from the viewer and back into the encoder."

  - trigger: ":thanks"
    replace: "Thanks, just let us know when you have a solid blue 4G light on your encoder"

  - trigger: ":tx"
    replace: "Can you please confirm the TX number on the side of the encoder? It will be fluctuating, but should not drop below 800 at any point."

  - trigger: ":mc"
    replace: "https://matchcentre.aus.frogbox.live/match/01"

  - trigger: ":setup"
    replace: "Hi, just checking if you're still planning on streaming on FrogBox today and how the setup is going?"

  - trigger: ":matchesadded"
    replace: |
      Thank you for your match request submission.
      Your match(es) have been added to our internal database and will be set up for FrogBox video.

  - trigger: ":whatsapp"
    replace: "Hi,\n\nWynand from FrogBox here. As mentioned, I am creating this group for your FrogBox streaming. We can use this group to troubleshoot issues if they arise throughout match days. Please notify us on this chat once you have turned the encoder on so we can check the vision.\n\nFeel free to add anyone else to this group if there's a need. You will see our support staff added to the group throughout the season.\n\n The WhatsApp chat is only an official support channel on Match Days, if you have any questions outside of that time, please submit a ticket to guarantee a response. Link here - https://frogbox.freshdesk.com/support/tickets/new\n\nCheers!"

  - trigger: ":saveandexit"
    replace: "Can you please ask your scorer to save and exit at the end of this over, then continue scoring as per usual"

  - trigger: ":signal"
    replace: |
      So with these encoders theres a few tips


      1) Make sure the antennas are on the correct port (theyre labelled, also best to have both on even if not using WiFi)

      2) Have the encoder as far off the ground as reasonable (ie. on a table or chair is fine)

      3) Have as little as possible between the encoders and the sky (ie. an umbrella is fine, or next to a window is better than at the back of the bar etc)

      4) Have the encoder at least 1m away from any concrete or metal
    force_clipboard: true

  - trigger: ":oneof2"
    replace: "If you have a solid blue 4G light, and we can't see it online, it is one of 2 issues\n\n1) The SIM doesn't have data on it\n\nOR\n\n2) The signal at the encoder is bad\rCan you please send us an image of the encoder and surroundings? We might see something we can suggest"
    force_clipboard: true

  - trigger: ":sim"
    replace: "The sim tray is super temperamental, so you might need to take the sim card out, put it in the slot, face the gold part down (it should just sit in the slot), turn the encoder 'upside down' and just geeeeeently slide the tray in\n\nhttps://youtu.be/j6Pqqmv8y-g"

  - trigger: xchan
    replace: Christian

  - trigger: fb
    replace: FrogBox

  - trigger: fnx
    replace: Frognostics

  - trigger: cnt
    replace: Controller

  - trigger: yt
    replace: YouTube
    word: true

  - trigger: wa
    replace: WhatsApp
    word: true

  - trigger: gfx
    replace: graphics

  - trigger: i
    replace: I
    word: true

  - trigger: lhal
    replace: "{{output}}"
    vars:
      - name: output
        type: random
        params:
          choices:
            - "Lets have a look"
            - "Checking for us"
            - "Let's check"
            - "Taking a look"
            - "Looking for you"

  - trigger: :dc
    replace: "The camera charges with the round DC plug on the opposite side to the screen. Not the USB cable"

  - trigger: :mute
    replace: "We send a blank 3.5mm cable you can plug into the mic port of the encoder, that will mute everything"

  - trigger: psm
    replace: Product Services Manager

  - trigger: :q
    replace: "During busy times in can take us a little while to respond, if you send additional messages in whatsapp it actually pushes you back , just FYI"

  - trigger: :nrq
    replace: "Doesnt look like we have a request in our system for you\n\nCan you give me 2 pieces of info please\n\n1) The link to the match for today and I'll set it up\n\n2) The ticket number with the request so I can see why we missed it"
    force_clipboard: true

  - trigger: :sorry
    replace: "Sorry for the delay, we are running into some issues with our servers that require manual intervention from us, so its made our response times slower than it should be"

  - trigger: :pw
    replace: 6$5@Evnoedbd!8^

  - trigger: "$$"
    replace: "£"
